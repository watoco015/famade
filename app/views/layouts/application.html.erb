<!DOCTYPE html>
<html>
  <head>
    <title>Famade</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
        <link href="https://fonts.googleapis.com/earlyaccess/nicomoji.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=M+PLUS+Rounded+1c" rel="stylesheet">

  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("famade_logo.png", class: :logo), root_path %>
          </div>
          <%# ログインしているときは以下を表示するようにしましょう %>
          <%# if user_signed_in? %>
             <%#div class="nav__right"> 
               <%# link_to "ログアウト", "#", data: { turbo_method: :delete }, class: :nav__logout %>
               <%# link_to "username", "#", class: :nav__btn %>
             <%#div> 
          <%# // ログインしているときは上記を表示するようにしましょう %>
          <%# else %>
          <%# ログインしていないときは以下を表示するようにしましょう %>
            <div class="nav__right">
              <%= link_to "ログイン", "#", class: :nav__btn %>
              <%= link_to "とうろく", "#", class: :nav__btn %>
            </div>
          <%# // ログインしていないときは上記を表示するようにしましょう %>
          <%# end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">faMade</p>
    </footer>
  </body>
</html>